# Suggested Commands
Windows / PowerShell project utilities:
- `rg --files` to list project files
- `Get-Content -Raw index.html` to read a file
- `Get-ChildItem -Force` to list directory contents
- `Start-Process index.html` to open the static page in the default browser

There is no package manager, build script, lint command, formatter config, or test command in the repo.